export type Category = {
  slug: string;
  name: string;
  shortName?: string;
  tagline: string;
  description: string;
  longDescription: string;
  heroImage: string;
  iconKey: string; // lucide icon name to render
};
